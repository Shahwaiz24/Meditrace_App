import 'package:flutter/material.dart';

class StartingDateView extends StatelessWidget {
  const StartingDateView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}